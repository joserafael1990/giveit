<p>
	<strong>Título:</strong>
	<%= @item.title %>
</p>
<p>
	<strong>Descripción:</strong>
	<%= @item.description %>
</p>

<h2>Solicitudes</h2>
	<%= render @item.requests %>

<h2>Ingresa una petición: </h2>
	<%= render 'requests/form' %>



<%= link_to 'Editar artículo', edit_item_path %> |
<%= link_to 'Atrás', items_path %>