<%= form_for(category) do |f| %>
  <% if category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(category.errors.count, "error") %> No se puede guardar esta categoría:</h2>

      <ul>
      <% category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label 'Nombre' %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit 'Procesar' %>
  </div>
<% end %>
